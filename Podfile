use_frameworks!

platform :ios, '8.0'

target 'GTUtils_Example' do
  pod 'GTUtils', :path => '../'

  target 'GTUtils_Tests' do
    inherit! :search_paths

    pod 'Specta'
    pod 'Expecta'
    pod 'FBSnapshotTestCase'
    pod 'Expecta+Snapshots'
  end
end
